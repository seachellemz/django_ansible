all:
    hosts:
        mzantsle.local:22024
    vars:
        ansible_user: ubuntu
        become: yes
        become_user: root
        repo_url: ssh://git@github.com
        repo:  seachellemz/django_ansible
        home_dir: /home/ubuntu
        repo_dir: "{{ home_dir }}/{{ repo }}"
        django_dir: "{{ repo_dir }}/django"
        static_dir: "{{ django_dir }}/static"
        django_project: yourproject
        dbname: django
        dbuser: django
        dbpassword: "{{ lookup('password', 'postgresqlpasswd') }}"
